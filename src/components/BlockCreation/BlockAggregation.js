import React from 'react';
import { Box, Typography } from '@mui/material';

function BlockAggregation({ proposedBlock }) {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>Block Aggregation</Typography>
      {/* Add aggregation logic here */}
    </Box>
  );
}

export default BlockAggregation;